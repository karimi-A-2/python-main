numbers = [1, 2, 3]

numbers = (1, 2)


# print(sum(numbers, 10))  # 1 + 2 + 10 = 13
#
# str_list = ['a', 'b', 'c']
# print('a' + 'b' + 'c')  # we can use + here to concatenate
# # but we can't use sum for concatenate str
# # wrong:
# res = sum(str_list)  # TypeError: unsupported operand type(s) for +: 'int' and 'str'
# res = sum(str_list, start='')  # TypeError: sum() can't sum strings [use ''.join(seq) instead]
# print(res)
