import numpy as np

a = np.array([1, 2, 3])
b = np.array([4, 5, 6])
a + b
a - b
a * b
b ** a
a + 4
a * 2
a ** 2
a.dtype
a / b  # different in pythons 2.x and 3.x
a // b
a = np.array([1.0, 2, 3])
a
a.dtype
a / b
a // b  # different in pythons 2.x and 3.x
a = np.array([1, 2, 3], dtype=np.float64)
a
a.dtype
